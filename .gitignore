# ★ 원본 수업자료는 절대 커밋하지 않는다.
#   - 출판 교재에서 온 자료가 섞여 있다 (CLAUDE.md §10)
#   - 3주차. Pandas(수업자료).ipynb 셀 4 에 Kaggle API 키가 평문으로 들어 있다
수업자료/

# 교재 파트 초고 — pandas.md 로 합친 뒤에는 필요 없다
docs/draft/

# 교재 검증이 만드는 파일 (to_pickle / to_csv / savefig 예제가 실제로 파일을 쓴다)
*.pickle
*.pkl
verify-out/

# webapp/test/expected.json 은 **커밋한다.**
# 실제 pandas 로 만든 교차 검증 기준값이라, 이게 있으면 pandas 없이도
# `npm run test:cross` 를 돌려 엔진이 pandas 와 일치하는지 확인할 수 있다.

# 파이썬
__pycache__/
*.pyc
.ipynb_checkpoints/

# node
node_modules/

# 편집기·OS
.vscode/
.idea/
Thumbs.db
Desktop.ini
