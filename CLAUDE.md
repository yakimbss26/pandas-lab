# Pandas Lab — 프로젝트 지침

> **배포 완료 (2026-08-04)**
> 저장소 https://github.com/yakimbss26/pandas-lab
> 학생용 사이트 https://yakimbss26.github.io/pandas-lab/
> 학생에게는 **사이트 URL 하나만** 준다.

`C:\pandas` 에서 **pandas 교재 + 실습 웹앱**을 만든다.
`C:\numpy` 의 NumPy Lab 에서 실제로 통한 방식을 그대로 따른다. 이 문서가 그 방식과
이 프로젝트에서 확인한 사실을 담은 단일 기준이다. **새 폴더를 만들 때마다 그 폴더에 `CLAUDE.md` 를 쓴다.**

---

## 1. 무엇을 만드는가 — 산출물 두 개

| 산출물 | 성격 | 파일 |
|:---|:---|:---|
| **교재** | 읽고 되돌아본다. 정의·수식·완결된 설명. | `pandas.md` |
| **실습 웹앱** | 조작해서 깨닫는다. 시뮬레이터·라이브 계산. | `index.html` + `pandas-lab.html` |

대상: **과학고 1학년**. 한국어.

**둘은 상호 보완이지 중복이 아니다.** 교재는 "왜 그런가"를 글로 완결하고, 웹앱은 글로 전달되지
않는 것(인덱스 정렬, 뷰와 복사, groupby 가 무엇을 묶는지)을 **실행으로** 보여 준다.
장 번호와 색 배정을 양쪽에서 일치시켜 학생이 오가며 볼 수 있게 한다.

**핵심 원칙 하나만 기억하면 된다: 화면의 숫자를 하드코딩하지 않는다.** 그 자리에서 계산한다.
그래야 학생이 슬라이더를 움직였을 때 진짜 값이 따라오고, 자료가 거짓을 가르치지 않는다.

---

## 2. 폴더 구조

```
C:\pandas\
  수업자료/                 ← 원본 입력. 읽기 전용. 절대 수정하지 않는다 (파일을 추가조차 하지 않는다)
  pandas.md                 ← 산출물 ① 교재
  index.html                ← 산출물 ② 웹앱 진입점        (빌드 생성)
  pandas-lab.html           ← 산출물 ② 단일 파일 배포본    (빌드 생성)
  package.json  README.md  LICENSE  .gitignore  .nojekyll
  CLAUDE.md                 ← 이 파일
  docs/                     ← Phase 0 산출물(정답표·정정표·인벤토리)
    CLAUDE.md
  webapp/
    API.md                  ★ 모듈 작성 계약 — 이 프로젝트에서 가장 중요한 문서
    build.js                빌드 (원본 → data.js, 인라이너)
    build_book.js           교재 합치기 (docs/draft/part*.md → pandas.md)
    serve.js                개발용 정적 서버 (npm start)
    CLAUDE.md
    src/
      theme.css  app.css    디자인 시스템 · 레이아웃
      core/
        df.js               미니 DataFrame 엔진
        ui.js               공용 위젯
        app.js              셸 (챕터 등록소·라우터·진도·테마)
        data.js             실습 데이터                   (빌드 생성)
        CLAUDE.md
      modules/
        chNN-*.js           장별 화면. 파일명 사전순 = 표시 순서
        CLAUDE.md
    test/
      df.test.js            엔진 단위 테스트          (164건 통과)
      gen_expected.py       실제 pandas 로 기대값 생성 → expected.json
      cross.test.js         expected.json ↔ 엔진 교차 검증  (88건 통과)
      verify_md.py          교재 코드 블록 실행 + 출력 대조 (`--fix` 로 출력 재생성)
      check_licensing.js    배포 산출물 저작권·자격증명 검사
      CLAUDE.md
```

---

## 3. 진행 순서

이 순서가 중요하다. 특히 **Phase 2 를 병렬화하지 말 것**.

### Phase 0 — 자료 추출과 사실 검증 (직접, 병렬 안 함) — **완료**

산출물은 `docs/` 에 있다. 재생성 스크립트는 스크래치패드에 있다.

1. `수업자료/` 훑기 → `docs/수업자료-인벤토리.md`
2. 원본 텍스트 추출 → 스크래치패드 `extracted/`. 이후 모든 에이전트의 입력.
3. **데이터 파일의 사실을 실제 pandas 로 계산한 정답표** → `docs/정답표.md`
   교재·웹앱의 모든 수치는 이 표가 기준이다. 손으로 적은 값은 신뢰하지 않는다.
4. 원본의 오류·구버전 API 목록 → `docs/정정표.md` (교재의 "원본에서 바로잡은 점" 절이 된다)

### Phase 1 — 교재 집필 (sonnet, 3개씩)

- 장을 5~7개 파트로 묶어 나눈다. 각 에이전트가 **파일 하나**를 쓰고 짧은 요약만 반환한다
  (본문을 반환값으로 받으면 컨텍스트가 터진다).
- 각 에이전트에게 주는 것: 추출된 원본 텍스트 경로, `docs/정답표.md`, `docs/정정표.md`,
  문체·형식 규칙, 배정 범위.
- 파트별로 **검증 에이전트**를 붙여 출력값을 다시 계산하고 파일을 직접 고치게 한다.
- 마지막에 메인이 파트 파일들을 머리말 + 목차와 함께 하나로 합친다.

### Phase 2 — 기반 직접 작성 (★ 병렬 금지, 메인이 직접)

**엔진 · 위젯 · 셸 · 빌드 · API.md 는 메인이 직접 쓴다.** 모든 화면이 이것들에 의존하므로,
API 가 흔들리면 전부 다시 써야 한다. 순서:

1. `core/df.js` — 미니 DataFrame (§8)
2. `test/df.test.js` — **즉시 검증.** 통과 전에는 다음으로 넘어가지 않는다
3. `theme.css` — 디자인 시스템 (§7)
4. `core/ui.js` — 위젯
5. `core/app.js` — 셸
6. `build.js`
7. **`API.md`** — 모듈 작성자가 쓸 수 있는 API 전부와 규칙. 병렬 작업의 성패를 가른다

### Phase 3 — 화면 모듈 병렬 (sonnet, 3개씩)

- 파일 하나 = 장 하나. `Lab.register({...})` 로 자기를 등록.
- 각 에이전트에게: **API.md 를 먼저 읽으라고 강제**, 배정 범위 상세, 완료 조건.
- 완료 조건에 `node --check <파일>` 와 `npm run build` 통과를 넣는다.

### Phase 4 — 브라우저 실검증 (직접)

**여기서 진짜 버그가 나온다.** 문법 검사와 node 하네스는 통과했는데 브라우저에서 죽는 경우가 있다(§5).

### Phase 5 — 배포

§10 참고. **공개 전 저작권·비밀 체크리스트를 반드시 통과시킨다.**

---

## 4. 에이전트 운영 규칙

| 규칙 | 이유 |
|:---|:---|
| **모델은 sonnet** | opus 로 11개 동시 실행 → 세션 한도로 **11/11 전부 실패**. sonnet 3개 배치 → 3/3 성공. |
| **3개씩 순차 배치** | 동시 실행 수를 줄이면 한도에 걸리지 않는다. 배치가 끝나면 다음으로. |
| **반환값은 요약만** | 본문·코드를 반환값으로 받으면 메인 컨텍스트가 터진다. 파일로 쓰고 경로만 받는다. |
| **schema 로 구조화 반환** | `{savedTo, lineCount, sections, simulators, syntaxChecked, concerns}` 처럼. |
| **완료 조건을 검사 명령으로** | "`node --check` 와 빌드를 통과했음을 확인한 뒤 보고하라". |
| **에이전트 보고를 그대로 믿지 말 것** | 아래 실제 사례들. |

### 에이전트 보고를 검증해야 하는 실제 사례 (NumPy Lab)

- "numpy 를 설치해 실행 검증했다" 보고 → `py -c "import numpy"` 로 확인하니 실패 →
  **의심했지만 실제로는 에이전트가 맞았다.** `py` 는 스텁이고 numpy 는 다른 경로에 있었다.
  → 교훈: 의심도 검증하고, 검증 도구 자체가 틀릴 수 있다.
- "라우터가 이중 렌더링된다" 보고 → 해시를 3번 강제 발행해 재현 시도 → **재현 안 됨.**
  자동화 도구 부작용이었다. 수정 불필요.
- 워크플로가 "11개 전부 실패" 보고 → 실제로는 **8개 파일이 이미 저장되어 있었다.**
  → 교훈: 실패 보고를 받으면 **파일 시스템을 직접 확인**하라.

### 이 프로젝트에서 추가로 배운 것

- **내 검증 스크립트가 라이브러리보다 먼저 틀린다.** 정정표 1차 실행에서 두 항목이
  실패했는데 원인은 pandas 가 아니라 (a) 항목들이 전역을 공유해 앞 항목이 `dict` 를 덮어썼고
  (b) matplotlib 미설치였다. → 각 검사는 **격리된 전역**에서 돌리고, `[EXCEPTION]` 이 뜨면
  라이브러리를 의심하기 전에 하네스를 먼저 의심한다.
- **"없어졌다"고 쓰기 전에 실행해 본다.** `agg(np.mean)`, `agg([max, min])` 은 없어졌을
  것 같지만 pandas 3.0.5 에서 **여전히 동작한다.** 추측으로 정정표를 쓰면 교재가 거짓을 가르친다.

---

## 5. 반드시 지킬 기술 규칙 (전부 실제로 깨져 본 것)

### ① `app.js` 를 모듈보다 **먼저** 로드한다

가장 비쌌던 버그. `app.js` 가 `window.Lab` 을 정의하는데 모듈이 로드 시점에 `Lab.register(...)` 를 부른다.
순서가 뒤바뀌면 모듈들이 **조용히 `Lab is not defined` 로 죽어** 장이 하나도 등록되지 않는다.
문법 검사도, node 하네스도 통과한다(하네스에서는 app.js 를 먼저 로드했으니까).
**브라우저에서 열어 보지 않으면 절대 발견하지 못한다.**

올바른 순서: `df → ui → data → app → modules → boot`

### ② ES 모듈 문법(`import`/`export`) 금지

단일 파일 배포본에 인라인되므로 깨진다. 모든 소스는 `window.X` 에 붙는 IIFE.
(외부 의존성·CDN 은 허용해도 이 규칙은 유지한다.)

### ③ `render(root)` 는 여러 번 호출된다

장을 다시 방문하면 다시 호출된다. 모듈 전역에 가변 상태를 두면 두 번째 방문에서 깨진다.
`render` 안의 지역 변수 + `rebuild()` 패턴을 쓴다.

### ④ 빌드는 원본이 없으면 기존 `data.js` 를 **보존**해야 한다

공개 저장소에서 `수업자료/` 를 제외하면, 클론한 사람이 빌드하는 순간 데이터가 전부 null 로
덮어써진다. `buildData()` 가 `null` 을 반환해 "기존 파일 유지"를 뜻하게 만들라.
**폴더를 숨긴 상태로 빌드해 실제로 시험하라.**

### ⑤ 표 위젯의 HTML escape 는 **열** 속성이다

`UI.table(cols, rows)` 에서 `raw: true` 는 `cols` 에 붙인다. 행에 붙이면 `<b>` 가 글자로 보인다.
전 장을 렌더해 태그 누출을 검사하는 스크립트를 한 번 돌려 두면 좋다.

### ⑥ 숫자를 하드코딩하지 않는다

엔진으로 계산해서 보여 준다. 이것이 이 방식의 존재 이유다.

---

## 6. 검증 4중 구조

교육 자료가 틀린 값을 가르치면 안 된다.

| 단계 | 무엇을 | 도구 |
|:--:|:---|:---|
| 1 | 엔진 단위 테스트 (기대값은 손계산) | `test/df.test.js` |
| 2 | **엔진 ↔ 실제 pandas 교차 검증** | `gen_expected.py` → `expected.json` → `cross.test.js` |
| 3 | 교재 코드 블록 실행 + 출력값 대조 | `verify_md.py`, `verify_outputs.py` |
| 4 | **브라우저 실조작** | 브라우저 도구로 클릭·입력해 값 확인 |

**2번이 가장 강한 보증이다.** 손계산 기대값은 사람의 착각이 섞이지만, 실제 pandas 가 낸 출력은 그렇지 않다.

### 검증 스크립트의 오탐을 예상하라

NumPy Lab 에서 `verify_outputs.py` 가 보고한 "값 불일치" 20건 중 **19건이 오탐**이었다. 원인:
- 출력 뒤에 붙은 **설명 주석**을 기대 출력으로 묶음
- 억제한 경고 줄이 실제 출력에 없음
- 교재가 버전별 값을 나란히 적어 둔 곳

→ **오탐 판별 기준을 `webapp/test/CLAUDE.md` 에 적어 두라.**

**이 프로젝트에서 미리 알고 있는 오탐 요인**: pandas 3.0 의 `ChainedAssignmentError` 는
**예외가 아니라 경고**다. 코드는 끝까지 돌고 결과만 반영되지 않는다. 검증 스크립트가
"경고 없음 = 정상" 으로 판정하면 이 케이스를 놓친다. 경고를 캡처해 대조하라.

---

## 7. 시각화 규칙

차트·그래프·대시보드를 만들기 전에 **`dataviz` 스킬을 로드**한다. 핵심만:

- **색 역할을 전 장에서 고정한다.** 학생이 색으로 역할을 익히므로 절대 바꾸지 않는다.
  배정은 §7-1 에 있다. **검증기를 돌려서 확정한 값이므로 임의로 바꾸지 않는다.**
- 크기(magnitude)는 **단일 색조 램프**만. 무지개 금지.
- **이중 y축 금지.** 스케일이 다르면 차트를 나눈다.
- 계열 2개 이상이면 범례 필수. 모든 점에 숫자를 찍지 않는다(의미 있는 것만).
- 모든 차트에 **표 보기 twin** — 툴팁이 값을 읽는 유일한 경로가 되면 안 된다.
- 상태색(good/critical)은 좋음·나쁨에만. 계열색으로 재사용 금지.
- 팔레트는 **검증기를 돌려서** 확정한다(색약 대비). 눈으로 판단하지 않는다.
- 라이트 모드에서 대비가 낮은 색 칸에는 **숫자 라벨을 반드시 함께** 보여 준다.

---

## 7-1. 색 배정 (검증기로 확정. 임의로 바꾸지 않는다)

`dataviz` 스킬의 `validate_palette.js` 를 실제로 돌려서 정한 값이다.

| 역할 | 라이트 | 다크 | 방식 |
|:---|:---|:---|:---|
| **원본** (original) | `#2a78d6` | `#3987e5` | 계열색 1 (파랑) |
| **사본** (copy) | `#eb6834` | `#d95926` | 계열색 2 (주황) |
| **결과** (result) | `#1baf7a` | `#199e70` | 계열색 3 (초록) |
| **강조** (highlight) | — | — | **색이 아니다.** 2px 링 + 굵은 라벨 |
| **에러/경고** | `#d03b3b` | `#d03b3b` | 상태색. **아이콘 + 라벨 필수**, 계열색으로 재사용 금지 |
| **결측 (NaN)** | `#898781` | `#898781` | 회색 + 45° 해칭 텍스처 |

표면: 라이트 `#fcfcfb`, 다크 `#1a1a19`. 잉크·격자선은 `theme.css` 참조.

### 왜 강조가 노랑이 아닌가 — 검증 결과

처음에 `강조=노랑(#eda100)` 으로 정했는데 **검증기가 두 모드 모두 FAIL 을 냈다.**

```
라이트 4색: [FAIL] 정상시야 #eda100↔#eb6834 ΔE 13.7 (하한 15)
다크  4색: [FAIL] CVD      #c98500↔#d95926 ΔE 4.8 (목표 8)
                 [FAIL] 정상시야                ΔE 10.6 (하한 15)
```

노랑과 주황이 한 화면에 있으면 색약자는 물론 **정상 시야로도 구분이 어렵다.**
그래서 강조를 색에서 빼고 **링 + 굵은 라벨**로 옮겼다. 남은 3색은 all-pairs 전부 통과한다.

```
라이트 3색: 전부 PASS (최악 CVD ΔE 9.2, 정상시야 24.0)
다크  3색: 전부 PASS (최악 CVD ΔE 9.4, 정상시야 20.9)
```

라이트 모드의 초록 `#1baf7a` 은 표면 대비 2.74:1 로 3:1 미만이라 **relief 규칙**이 걸린다 —
숫자 라벨 또는 표 보기를 반드시 함께 낸다. 이 프로젝트는 이미 "모든 차트에 표 보기 twin" 을
규칙으로 두었으므로 충족된다.

**계열이 4개 이상 필요한 화면이 나오면** 색을 더 만들지 말고 (a) 하나로 묶어 "그 외" 로 접거나
(b) 차트를 나눈다(small multiples). 팔레트에 색을 추가하려면 검증기를 다시 돌려야 한다.

---

## 8. 미니 엔진 `df.js` — pandas 의 계산 핵심

NumPy Lab 의 힘은 `nd.js` 가 NumPy 와 똑같이 `(buffer, shape, strides, offset)` 로 배열을
모델링한 데서 나왔다. pandas 에서 대응되는 질문은 **"글로는 전달되지 않는데 코드로 모델링하면
보여줄 수 있는 것이 무엇인가?"** 이고, 답은 이것이다.

`df.js` 는 DataFrame 을 **`{index, columns, blocks, dtypes, refs}`** 로 모델링한다.
컬럼별 배열 + 인덱스 객체 + dtype + **참조 카운트(Copy-on-Write 추적)**.

이 모델이 있으면 다음을 실행으로 보여줄 수 있다.

| 보여줄 것 | 엔진에서 어떻게 |
|:---|:---|
| **뷰와 복사, Copy-on-Write** | 참조 카운트를 화면에 띄우고, 쓰기 순간 블록이 복사되는 것을 애니메이션 |
| **인덱스 정렬(alignment)** | 두 Series 의 인덱스를 나란히 놓고 합집합·NaN 생성 과정을 단계 실행 |
| **중복 인덱스가 행을 늘리는 것** | `e` 가 두 줄이 되는 과정을 짝짓기로 시각화 |
| **groupby 가 무엇을 묶는지** | split → apply → combine 3단계를 실제 행 묶음으로 표시 |
| **dtype 승격** | `fillna('0')` 한 번에 float64 → str 로 넘어가며 연산이 죽는 과정 |
| **loc / iloc 차이** | 인덱스 라벨과 위치를 동시에 표시하고 같은 숫자가 다른 행을 가리키는 순간 |
| **merge 의 키 매칭** | 좌/우 키를 선으로 잇고 how 를 바꿀 때 선이 늘고 줄는 것 |

### 장 구성 원칙

- 장 구성은 **원본 수업자료의 순서를 기준으로** 잡는다. 자료에 있는 것이 최소 범위다.
- **학생이 가장 많이 틀리는 개념 3개를 골라 거기에 가장 많은 공을 들인다.**
  이 프로젝트의 3개: **① 뷰/복사와 연쇄 할당 ② 인덱스 정렬 ③ loc/iloc**. (근거는 `docs/정정표.md`)
- 장마다 시뮬레이터 최소 2개 + 확인 문제 2~4개.

---

## 9. 환경 (2026-07-30 이 기계에서 실제 확인)

| 항목 | 확인된 값 |
|:---|:---|
| **파이썬** | `C:\Users\user\AppData\Local\Programs\Python\Python313\python.exe` (3.13.12) |
| `py` / `python` / `python3` | **Microsoft Store 스텁. 쓰지 말 것.** 위 절대 경로를 쓴다 |
| pandas / numpy | **3.0.5** / 2.5.1 |
| matplotlib | 3.11.1 (설치함). **seaborn 은 없음** — 원본의 `sns.distplot`/`sns.boxplot` 은 재현 불가 |
| node / npm | v24.18.0 / 11.16.0 |
| git / gh | 설치됨 (`C:\Program Files\Git`, `C:\Program Files\GitHub CLI`) |
| git 저장소 | `main` 브랜치, `origin` = github.com/yakimbss26/pandas-lab (public) |
| scikit-learn / scipy | 1.9.0 / 1.18.0 (14장 검증용으로 설치함) |
| pypdf | 설치함 (PDF 텍스트 추출) |

| 함정 | 대처 |
|:---|:---|
| 파이썬 한글 출력 깨짐 | `-X utf8` 플래그 |
| matplotlib 창이 뜨며 멈춤 | `$env:MPLBACKEND = "Agg"` |
| `.ipynb` / `.pptx` 추출 | node 로 한다. pptx 는 zip → PowerShell `System.IO.Compression.ZipFile` |
| PDF | `pdftoppm` 이 없어 Read 도구의 PDF 렌더링 불가 → `pypdf` 로 텍스트만 뽑는다 (설치함) |
| 한글 폴더명이 URL·git 에서 문제 | 웹에서 서빙되는 경로는 **ASCII 로만** (`webapp/src/...`). 한글 폴더는 입력 전용, 배포 제외 |
| 교재 검증이 파일을 만든다 | `to_pickle`/`to_csv` 예제가 실제로 파일을 쓴다. `.gitignore` 에 넣어라 |
| `sleep` 으로 대기 | 금지됨. 조건 폴링은 `until` 루프로 |

---

## 10. 배포 (GitHub Pages)

```bash
npm install
npm run build
git init && git add -A && git commit -m "..."
gh repo create <이름> --public --source=. --remote=origin --push
gh api -X POST repos/<계정>/<이름>/pages -f "source[branch]=main" -f "source[path]=/"
```

- `index.html` 이 저장소 루트에 있어야 한다. `.nojekyll` 을 넣는다.
- **빌드 산출물(`index.html`, `pandas-lab.html`, `data.js`)을 커밋해야 한다.** Pages 는 정적 파일만 서빙한다.
- Pages 빌드는 30~60초. `until` 루프로 HTTP 200 을 폴링해 확인한다.
- **배포 후 실제 URL 을 브라우저로 열어 검증하라.**
- 학생에게는 **Pages URL 하나만** 준다 (Pages 는 `.md` 를 `text/markdown` 으로 내주므로 원문이 그대로 뜬다).

### 공개 전 저작권·비밀 체크리스트 ★

**이 프로젝트에서 이미 발견된 것들. 저장소를 만들기 전에 반드시 처리하라.**

0. **★★ 자격증명 유출** — `수업자료/3주차. Pandas(수업자료).ipynb` 셀 4 의 저장된 출력에
   **Kaggle API 사용자명과 키가 평문으로 들어 있다.**
   Colab 의 업로드 헬퍼가 업로드한 파일 내용을 그대로 출력해서 노트북에 저장된 것이다.
   같은 계열 노트북의 업로드 셀 출력도 확인하라.
   → 저장소에 절대 넣지 않는다. **키 소유자에게 폐기(revoke)를 알려야 한다.**
   → **이 문서에도 사용자명·키를 옮겨 적지 마라.** 유출 사실과 위치만 적는다.
     사용자명만 공개돼도 "누구의 키를 찾으면 되는지" 를 알려주는 셈이다
     (전수 검사에서 실제로 이 문서가 걸렸다).
1. **출판 교재에서 온 자료** — 제외한다. 해당 파일:
   `머신러닝회귀모델（전복）.pdf` (교사용 PPT: "생각 열기/학습 목표/활동 평가" 구조),
   `1. 데이터 처리-학생용.ipynb` (본문에 `P.13`~`P.35` 페이지 참조),
   `데이터 핸들링 - 판다스.ipynb` 계열 (권철민 『파이썬 머신러닝 완벽 가이드』 예제 구성).
2. **재배포 제한 데이터** — Kaggle 대회 데이터(`train.csv`/`test.csv`/`gender_submission.csv` = Titanic),
   `ramen-ratings.csv`(Kaggle 재업로드), `abalone.csv`(Kaggle 재업로드본, 컬럼명 한글화됨).
   → 라이선스를 직접 확인하고, 확실하지 않으면 합성 데이터로 대체한다.
   **`lab_earthquake.csv` 는 USGS = 미국 정부 저작물 = 퍼블릭 도메인이므로 그대로 써도 된다.**
   `housing.data` 는 Boston Housing 으로, 인종 관련 파생 변수(`B`) 문제로 sklearn 에서 퇴출된
   데이터다. 교육용으로 쓸 경우 그 사실을 밝히거나 다른 데이터로 바꾼다.
3. **★ 빌드 산출물에 원본 데이터가 임베드되는지 확인** — 가장 놓치기 쉽다.
   폴더를 `.gitignore` 로 제외해도 `data.js` 와 단일 파일 배포본에 값이 박혀 있으면 재배포가 된다.
   → **구조만 같은 합성 데이터로 대체**하고, 화면에 합성이라는 사실을 밝힌다.
   → 합성으로 바꾼 뒤 **원본의 특징적인 값을 grep 해서 0건임을 확인**한다.
     (검사할 문자열 예: `Braund`, `Heikkinen`, `New Touch`, `Kamfen`, 그리고 위 API 키)
4. **집계값·요약 통계는 사실이므로 그대로 써도 된다** (행 수, 평균 등). 데이터 자체가 아니다.
5. `LICENSE` 에 코드 라이선스와 **서드파티 데이터 조항을 분리해** 적는다.

---

## 11. 정책 결정

2026-07-30 사용자 확정.

| 항목 | 결정 |
|:---|:---|
| 외부 의존성 설치 | **허용** (`npm install`, `pip install`) |
| 배포 형태 | **둘 다.** `index.html` 이 기본, `pandas-lab.html` 단일 파일도 계속 생성 |
| CDN 링크 | **허용.** 단 없어도 나머지가 동작해야 한다(지연 로딩 + 실패 안내) |
| 저장소 공개 | **public + GitHub Pages** |
| 원본 자료 포함 | **제외.** `수업자료/` 전체를 `.gitignore` |
| 실습 데이터 | **합성 대체 + USGS 는 실데이터.** 타이타닉·라멘·전복은 구조만 같은 합성 데이터로 바꾸고 **화면에 합성임을 명시**한다. `lab_earthquake.csv` 는 퍼블릭 도메인이라 실데이터를 쓴다 |
| ML 노트북 범위 | **마지막 1장만.** pandas 14장 + "pandas 에서 머신러닝으로" 1장 |

---

## 11-1. 장 구성 (확정)

★ 표시가 §8 의 "가장 많이 틀리는 3개". 여기에 가장 많은 공을 들인다.

| 장 | 제목 | 주 출처 |
|--:|:---|:---|
| 1 | pandas 는 무엇을 해결하는가 | nb01 리스트 vs 배열, nb05 도입 |
| 2 | 데이터를 불러오고 첫눈에 파악하기 | nb05 라멘, nb07 타이타닉 |
| 3 | Series — 인덱스를 가진 1차원 | nb05 [78]~[92] |
| 4 | DataFrame — 여러 Series 의 집합 | nb05 [93]~[98], nb07 [50]~[57] |
| 5 | ★ loc 와 iloc — 라벨과 위치 | nb07 [83]~[107] |
| 6 | ★ 인덱스 정렬 — 연산은 인덱스로 짝짓는다 | nb05 [141]~[158] |
| 7 | ★ 뷰와 복사, Copy-on-Write | 정정표 B 전체, `Numpy 얇은 복사.pdf` |
| 8 | 결측 데이터 | nb05 [20]~[40], nb07 [132]~[137] |
| 9 | 값 바꾸기와 타입 변환 | nb07 [141]~[147], nb05 [168]~[170] |
| 10 | 정렬과 집계 | nb07 [108]~[131] |
| 11 | groupby — split · apply · combine | nb05 [187]~[195], nb07 [117]~[131] |
| 12 | 표를 합치기 (concat · merge) | **원본에 없다.** 필수 개념이라 추가 |
| 13 | 노트북이 거짓말할 때 — 실행 순서와 상태 | nb06 `titanic.ipynb` (정정표 D-1) |
| 14 | 종합 실습 + pandas 에서 머신러닝으로 | nb03/nb04 USGS 과제, nb08~nb10 |
| 부록 | 원본에서 바로잡은 점 | `docs/정정표.md` |

---

## 12. 수업자료 인벤토리 요약

전체는 `docs/수업자료-인벤토리.md`. 요약:

- **노트북 14개**(중복·체크포인트 포함). 핵심은 3개:
  `3주차. Pandas(수업자료).ipynb`(199셀, 강의본), `3주차 학생 Pandas(수업자료).ipynb`(162셀, 빈칸본),
  `데이터 핸들링 - 판다스.ipynb`(159셀, 가장 상세).
- **체크포인트가 정답 원본이다.** 본 노트북의 `pass` 빈칸 자리에 실제 코드가 남아 있다
  (`df_dict.values.tolist()` 등 5곳). 플레이북이 예고한 그대로.
- `titanic.ipynb` 는 **오류가 그대로 저장된 라이브 코딩** 기록이다. 실행 순서 뒤섞임,
  `dt = DecisionTreeClassifier` (괄호 누락) 로 인한 `fit() missing 'y'`, `fillna('0')` 로 dtype 붕괴,
  오타 컬럼 `Embarkded`. **교재의 "왜 내 노트북은 어제 되던 게 오늘 안 되는가" 장 재료로 최상급.**
- 데이터 7개: `ramen-ratings.csv`(2580×7), `train.csv`(891×12), `test.csv`(418×11),
  `gender_submission.csv`(418×2), `abalone.csv`(4177×8, 컬럼명 한글), `housing.data`(506×14),
  `lab_earthquake.csv`(467×22).
- 원본 노트북 기준 pandas 버전: **1.1.5 / 1.3.4**. 현재는 3.0.5 → 정정 필요 항목 다수.

---

## 13. NumPy Lab 실적 (참고 기준선)

| 항목 | 규모 |
|:---|:---|
| 교재 | 12장, 4,277줄, 278 KB |
| 웹앱 | 11장, 시뮬레이터 30여 개 |
| 미니 엔진 | 약 1,100줄 |
| 화면 모듈 | 약 8,000줄 (장당 500~1,400줄) |
| 단일 파일 배포본 | 812 KB |
| 검증 | 단위 187건 + 실제 라이브러리 교차 151건 + 교재 코드 165블록 |
| 저장소 / 사이트 | https://github.com/yakimbss26/numpy-lab · https://yakimbss26.github.io/numpy-lab/ |

`C:\numpy` 를 **참조 구현으로 열어 보라.** 그대로 재사용 가능한 틀:

- `webapp/API.md` — 모듈 작성 계약 (구조를 베끼고 API 목록만 교체)
- `webapp/src/theme.css` — 디자인 시스템 (검증된 팔레트. 거의 그대로)
- `webapp/src/core/ui.js` — 위젯 (주제와 거의 무관. 그대로 가져다 쓰라)
- `webapp/src/core/app.js` — 셸 (그대로)
- `webapp/build.js` — 빌드 (경로와 데이터 생성부만 교체)
- `webapp/test/*` — 검증 4중 구조의 실제 구현

---

## 14. 원본 플레이북

이 문서는 `교육자료-제작-플레이북.md` 를 이 프로젝트에 맞게 구체화한 것이다.
다른 주제로 재사용할 일반형 플레이북이 필요하면 이 파일에서 §9·§12 (환경·인벤토리)와
§10 의 저작권 항목을 비우고, `pandas`/`df.js` 를 `<주제>`/`<engine>.js` 로 되돌리면 된다.
